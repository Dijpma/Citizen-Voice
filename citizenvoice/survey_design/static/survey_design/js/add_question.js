$(document).ready(
    function() {
        let button_add_survey = $('#sidebar-left-button-add-question')
        button_add_survey.on("click", function(event) {
            alert("Clicked add question button")
        });
    }
);
